コメント編集画面 ユーザー情報を置いておきたい
<div class="row">
    <%= form_with model:@comment, local:true do |f| %>
        <%= f.label :comment %>
        <%= f.text_area :comment, autofocus: true, autocomplete: 'ノボログ' %>
        <%= f.submit "編集", class:"btn btn-primary" %>
    <% end %>
</div>
    <%= link_to "戻る", comment_path(@comment) %>
