<% gym.comments.each do |comment| %>
  <tr>
    <td>
      <%= comment.users_comment_count %>回目の来店
      <%= link_to (comment.user) do %>
        <%= attachment_image_tag comment.user, :profile_image, size:'50x50', format: 'jpeg', class: "rounded-circle", fallback: "default_user.jpg" %>
        <%= comment.user.name %>
      <% end %>
    </td>
    <td><%= comment.created_at.strftime('%Y/%m/%d') %></td>
    <td><%= comment.comment %></td>
    <% if comment.user == current_user %>
      <td><%= link_to "削除", gym_comment_path(comment.gym,comment), class: "btn btn-danger pull-right", data: {confirm: "Are you sure?"}, method: :delete, remote:true %></td>
    <% end %>
  </tr>
  <% end %>
</div>
